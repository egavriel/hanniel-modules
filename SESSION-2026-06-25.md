# Hanniel — Session 2026-06-25 continuation note

## Where we are

Shipped today (verified end-to-end):
- `hanniel-invoice/SKILL.md` rewritten — single canonical procedure (`/api/invoice/store` → local Puppeteer → deliver)
- `hanniel-invoice-api/SKILL.md` patched — `/api/invoice/generate` demoted to "deprecated for agents"
- `local-invoice-puppeteer.js` modified — loads catalog from `shared/catalog.json` (D1 mirror), `MASTER_DATA_FALLBACK` only as offline safety net
- `~/.hermes/hanniel-scripts/sync-catalog.js` (NEW) — refreshes D1 mirror
- `~/.hermes/hanniel-scripts/render-orders-md.js` (NEW) — regenerates `orders-YYYY-MM-DD.md` from D1
- Skills created: `hanniel-render-orders-md`, `hanniel-sync-catalog`, `hanniel-invoice-delivery-scopes`

Smoke test passed: `LH-2606-0007` allocated, rendered to 84KB 1191×1685 JPEG.

## In-flight (NOT shipped, awaiting confirmation)

### Delivery-date feature — paused for review

User constraint: only `hanniel-dp` should show delivery date. `hanniel` regular is meaningless (often generated the day AFTER delivery), `hanniel-fi` is already the delivery day.

Implementation done in `local-invoice-puppeteer.js`:
- `--delivery-date YYYY-MM-DD` CLI flag wired through `parseCliArgs` → `calcOpts.delivery_date` → `result.delivery_date`
- Rendered only on hanniel-dp, appended to existing Notes block as "Delivery scheduled: 22 Dec 2026." in `config.textColor`
- Verified: `/root/preview/dp_with_delivery.jpg` (107KB) shows it, `/root/preview/regular_with_delivery.jpg` (84KB) shows nothing extra

**NOT YET DONE:**
- Migration `0004_add_delivery_date.sql` (ALTER TABLE invoices ADD COLUMN delivery_date TEXT)
- Apply to remote D1
- `functions/api/invoice/store.ts` accepts `delivery_date` field
- `functions/_shared/db.ts` `createInvoice`/`getInvoice` store/return new field
- UI date picker (only on hanniel-dp form section)
- Update `hanniel-invoice` skill with `--delivery-date` CLI flag docs
- Update `hanniel-invoice-api` with the new field in `POST /api/invoice/store` body
- `render-orders-md.js` to surface delivery date

## Architectural rules to preserve

- **Cloudflare Browser Rendering is BANNED for image generation.** Data ops only via CF.
- **Two invoice paths share data, may diverge on render:** (1) Agent (WA chat → me): `/api/invoice/store` + local Puppeteer + WA bridge. (2) UI (browser → app.hanniel.co/invoicing): `/api/invoice/store` + html2canvas + browser download. Don't couple the renderers.
- **Pre-orders: defer pre-order infrastructure.** Use hanniel-dp+hanniel-fi dated today, delivery date in `notes` field as the workaround. Only `hanniel-dp` cares about delivery_date (see above).
- **Catalog D1 is canonical.** Mirror via `~/.hermes/hanniel-scripts/sync-catalog.js`. Renderer reads from `/root/hanniel-modules/shared/catalog.json` (auto-discovered, with `MASTER_DATA_FALLBACK` as offline safety net).

## Things discovered this session worth noting

- D1 `/api/invoices?from=&to=` filter is broken (returns all invoices regardless of date). `render-orders-md.js` works around by filtering client-side.
- Smoke test `/api/invoice/store` allocates `LH-2606-NNNN` atomically via UPSERT — no manual sequencing needed.
- Cloudflare Pages Function `/api/invoice/store` works without auth, returns `{invoice_no, invoice_date, id, status}`.
- WEUR D1 (`kongsian-db` / `94a52c2f-...`) was deleted this morning. APAC HKG (`kongsian-db-apac` / `477704de-...`) is the only one.

## Memory state

3 entries (38%): timezone rule, Kongsian config, M3 budget. Hanniel facts removed — they live in the skills now.